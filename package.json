{
  "name": "vc-chrome-tab",
  "version": "0.1.0",
  "private": true,
  "description": "Toby-style tab manager — Chrome extension (MV3) + cloud sync backend",
  "scripts": {
    "dev": "pnpm --filter @vctabs/extension dev",
    "build": "pnpm --filter @vctabs/extension build",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "@node-rs/argon2"]
  }
}
