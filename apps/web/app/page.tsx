import { Shell, Title } from "./_ui";

export default function Home() {
  return (
    <Shell>
      <Title sub="Cloud-sync backend for the VC Tabs browser extension. Email verification and password reset are handled here; sign-in itself happens inside the extension.">
        VC Tabs
      </Title>
    </Shell>
  );
}
